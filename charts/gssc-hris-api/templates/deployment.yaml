apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "gssc-hris-api.fullname" . }}
  labels:
    {{- include "gssc-hris-api.labels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "gssc-hris-api.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "gssc-hris-api.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "gssc-hris-api.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 80
              protocol: TCP
          lifecycle:
            postStart:
              exec:
                #command: ["composer", "update", "--no-scripts", "--no-interaction"]
                command:
                  - "sh"
                  - "-c"
                  - |
                    composer update && php artisan passport:install




          volumeMounts:
            - mountPath: /var/www/html
              name: gssc-hris-dev
              subPath: app/api/
                #livenessProbe:
                #httpGet:
                #path: /
                #port: http
                #readinessProbe:
                # exec:
                #command: ["/bin/sh", "-c",  "composer", "update", "--no-scripts", "--no-interaction"]
                #initialDelaySeconds: 30
                #periodSeconds: 2
                #timeoutSeconds: 1
                #successThreshold: 3
                #failureThreshold: 10
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      volumes:
        - name: gssc-hris-dev
          persistentVolumeClaim:
            claimName: "{{ .Values.volumes.claimName }}"
