apiVersion: v1
kind: Service
metadata:
  name: {{ include "gssc-hris-api.fullname" . }}
  labels:
    {{- include "gssc-hris-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gssc-hris-api.selectorLabels" . | nindent 4 }}
